fn catch_rain_brute_force(heights: &Vec<u32>) -> u32 {
    let len = heights.len();
    if len < 1 {
        return 0;
    }
    for i in 1..len {
        
    }
    return 0;
}

#[cfg(test)]
mod test {
    use super::*;
    #[test]
    fn test_catch_rain() {

    }
}